interface StatCardProps { title: string; value: number | string; icon: React.ReactNode; color: 'blue' | 'amber' | 'green' | 'red' | 'purple'; subtitle?: string; trend?: { value: string; positive: boolean }; onClick?: () => void }
const colors = { blue: 'bg-blue-100 text-blue-700 dark:bg-blue-950/60 dark:text-blue-300', amber: 'bg-amber-100 text-amber-700 dark:bg-amber-950/60 dark:text-amber-300', green: 'bg-emerald-100 text-emerald-700 dark:bg-emerald-950/60 dark:text-emerald-300', red: 'bg-red-100 text-red-700 dark:bg-red-950/60 dark:text-red-300', purple: 'bg-violet-100 text-violet-700 dark:bg-violet-950/60 dark:text-violet-300' }
export default function StatCard({ title, value, icon, color, subtitle, trend, onClick }: StatCardProps) {
  return <div onClick={onClick} className={`rounded-xl border border-border bg-card p-5 text-card-foreground shadow-sm transition-all ${onClick ? 'cursor-pointer hover:-translate-y-0.5 hover:shadow-md' : ''}`}><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="text-sm font-medium text-muted-foreground">{title}</p><p className="mt-2 text-3xl font-semibold tracking-tight text-foreground">{value}</p>{subtitle && <p className="mt-2 text-xs text-muted-foreground">{subtitle}</p>}{trend && <p className={`mt-2 text-xs font-semibold ${trend.positive ? 'text-[var(--success)]' : 'text-destructive'}`}>{trend.value}</p>}</div><div className={`flex size-11 shrink-0 items-center justify-center rounded-xl ${colors[color]}`}>{icon}</div></div></div>
}
